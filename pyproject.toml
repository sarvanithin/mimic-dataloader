[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mimic-dataloader"
version = "0.1.0"
description = "Drop-in PyTorch Dataset classes for standard MIMIC-IV prediction tasks."
readme = "README.md"
authors = [{ name = "Nithin Sarva", email = "nithin@nithinsarva.com" }]
license = { text = "MIT" }
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
]
keywords = ["pytorch", "dataloader", "mimic-iv", "ehr", "clinical-prediction"]
dependencies = [
    "streamlit>=1.32.0",
    "requests>=2.31.0",
    "pandas>=2.0.0",
]
requires-python = ">=3.9"

[project.optional-dependencies]
ml = [
    "torch>=2.0.0",
    "numpy>=1.20.0",
    "scikit-learn>=1.0.0",
]
dev = [
    "pytest>=7.0.0",
    "black>=23.0.0",
    "isort>=5.0.0",
    "flake8>=6.0.0",
]

[tool.setuptools.packages.find]
include = ["mimic_dataloader", "mimic_dataloader.*"]

[tool.black]
line-length = 88
target-version = ['py39']

[tool.isort]
profile = "black"
