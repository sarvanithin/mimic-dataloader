services:
  - type: web
    name: mimic-dataloader
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "8000"
      - key: MPP_SECRET_KEY
        value: mimic-dev-secret
